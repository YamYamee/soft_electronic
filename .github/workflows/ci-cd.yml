name: Fast CI/CD

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, runner]
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: 빠른 배포
        run: |
          cd ${{ github.workspace }}
          echo "🚀 초고속 배포 시작 (Docker 건너뛰기)"
          # 디스크 공간 확인
          echo "디스크 공간 확인..."
          df -h

          # Docker 및 시스템 공간 정리
          echo "도커 컨테이너, 이미지, 볼륨, 네트워크 정리 중..."
          docker container prune -f || true
          docker image prune -af || true
          docker volume prune -f || true
          docker network prune -f || true
          echo "사용하지 않는 패키지 및 캐시 정리 중..."
          sudo apt-get clean || true
          sudo apt-get autoremove -y || true
          rm -rf ~/.cache/pip || true
          echo "공간 정리 완료"

          # Python 직접 실행 모드
          echo "Python 직접 실행 모드"

          # Python 환경 설정 (pip 설치)
          python3 -m ensurepip --upgrade || sudo apt-get install python3-pip -y
          python3 -m pip install --upgrade pip --user --break-system-packages
          pip3 install -r requirements-minimal.txt --user --break-system-packages

          # 기존 프로세스 종료
          pkill -f "python.*websocket_server.py" || true
          pkill -f "websocket_server" || true

          # 포트 8000 사용 중인 프로세스 종료
          sudo lsof -ti:8000 | xargs -r sudo kill -9 || true

          # 서버 자동 실행 (이미 실행 중이면 재시작)
          nohup python3 ./websocket_server.py > server.log 2>&1 &

          echo "서버 시작 중..."
          sleep 10

          # 헬스체크
          curl -f http://localhost:8000/health || echo "헬스체크 실패했지만 계속 진행"
            - name: 빠른 배포
              run: |
                cd ${{ github.workspace }}
                echo "🚀 초고속 배포 시작 (Docker 건너뛰기)"
                # 디스크 공간 확인
                echo "디스크 공간 확인..."
                df -h
                # ...existing code...

            - name: 웹소켓 서버 자동 실행
              run: |
                cd ${{ github.workspace }}/soft_electronic/soft_electronic
                nohup python3 websocket_server.py > server.log 2>&1 &
                echo "서버 시작 중..."
                sleep 10
